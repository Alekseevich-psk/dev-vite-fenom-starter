# ⚡️ Vite + Fenom.js — Статический сайт-генератор для верстки и прототипирования

- ⚡ **Vite** — высокая скорость сборки и HMR
- 💡 **Fenom.js** — шаблонизатор с синтаксисом, близким к Fenom для PHP (MODX Revo)
- 🧩 Поддержка `{extends}`, `{include}`, `{if}`, `{foreach}`, фильтров и переменных
- 🎨 SCSS + автопрефиксер
- 🖼️ Автоконвертация `.ttf` → `.woff` / `.woff2`
- 📦 Простая интеграция шрифтов
- 🌐 Асинхронный `wget`-инструмент для скачивания удалённых файлов
- ✅ Готов к локальной верстке без сервера

## 🪄 Запуск и работа

```sh
- git clone https://github.com/Alekseevich-psk/dev-vite-fenom-starter
- npm i 
- npm run dev
- npm run build
```

## 🖋️ Шаблонизация с Fenom.js

- Синтаксис, близкий к `fenom.php` (MODX Revo)
- Поддерживает:
- - Переменные: **{$var}**
- - Условия: **{if}**
- - Поддержка: **==, !=, >, <, >=, <=, ===, !==, !, &&, ||, in** (для массивов).
- - Тернарные и логические операторы: **{$age >= 18 ? 'Да' : 'Нет'}**
- - Циклы **{foreach}** + Поддержка вложенных циклов.
- - Фильтры: Применяются через **|** (как в оригинальном Fenom):
- - Исключение: **{ignore}**
- - Комментарии: **{ * комментарий * }**
- Работает **в браузере** и **в Node.js**
- В браузере: **без `include` и `extends`** (ограничение среды)
- Легко внедряется в HTML-страницы для локальной верстки

### 🧩 Установка fenom.js (если используется отдельно)
```bash
npm install fenom-js
```

### 🧰 vite-plugin-fenom

```bash
npm add vite-plugin-fenom -D
```

```ts
import { defineConfig } from 'vite';
import fenom from 'vite-plugin-fenom';

export default defineConfig({
  plugins: [
    fenom({
      pages: 'src/pages/',           // Путь к .tpl-страницам
      data: 'src/data/**/*.json',    // JSON-контексты
      root: 'src/',                  // Корень исходников
    }),
  ],
  build: {
    outDir: 'dist',
    emptyOutDir: true,
    rollupOptions: {
      input: ['src/scripts/main.ts', 'src/styles/style.css'],
    },
  },
});
```

## 🚩 Быстрое подключение шрифтов

-   Шрифты формата .ttf закидываем по адресу `"public/fonts/ttf/"`
-   Запускаем конвертацию в формат .woff(.woff2) `npm run ttfToWoff`
-   Подключаем шрифты в mixin scss `"public/styles/fonts.scss"` командой `npm run fontsInStyle`

## ❗️ WGET

-   Сверх простой асинхронный поиск удаленных файлов по протоколам http или https, вдохновленный wget js 
```js
    node tasks/wget url
```

## :point_right: Нюансы

-   Данную сборку использую для личных проектов, могут присутствовать наработки под быстрый старт проекта или тестовые модификации

## ⚠️ Сборка собрана на версии node.js - 23.0.0